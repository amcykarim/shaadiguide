const vowBuilder=document.querySelector('[data-vow-builder]');if(vowBuilder){const form=vowBuilder.querySelector('form');const output=vowBuilder.querySelector('[data-builder-output]');const val=name=>form.elements[name]?.value.trim()||'';const sentence=text=>text?text.replace(/[.!?]?$/,'.'):'';
function build(event){event?.preventDefault();const names=val('coupleNames')||'[name]';const tone=val('tone')||'warm';const opening=tone==='formal'?`${names}, I make these personal promises with care and gratitude.`:`${names}, I choose you with joy and with a clear understanding that a strong marriage is built day by day.`;const values=val('values')?`I will protect the values we share: ${sentence(val('values'))}`:'';const memory=val('memory')?`I carry with me the memory of ${sentence(val('memory'))}`:'';const promises=val('promises')?`I promise to ${sentence(val('promises')).replace(/^./,c=>c.toLowerCase())}`:'I promise to listen honestly, speak kindly and keep learning beside you.';const goals=val('goals')?`I look forward to ${sentence(val('goals')).replace(/^./,c=>c.toLowerCase())}`:'';const family=val('family')?`I will honour ${sentence(val('family')).replace(/^./,c=>c.toLowerCase())}`:'';const close=val('closing')==='traditional'?`With respect for our families and hope for our shared future, I offer you these promises.`:`This is the partnership I choose, today and in all the ordinary days ahead.`;output.textContent=[opening,values,memory,promises,goals,family,close].filter(Boolean).join('\n\n')}
form.addEventListener('submit',build);form.addEventListener('input',build);form.addEventListener('reset',()=>setTimeout(build,0));vowBuilder.querySelector('[data-builder-print]')?.addEventListener('click',()=>{build();window.print()});build()}
