const speechBuilder=document.querySelector('[data-speech-builder]');if(speechBuilder){const form=speechBuilder.querySelector('form');const output=speechBuilder.querySelector('[data-builder-output]');const val=name=>form.elements[name]?.value.trim()||'';const sentence=text=>text?text.replace(/[.!?]?$/,'.'):'';
function build(event){event?.preventDefault();const names=val('coupleNames')||'[couple names]';const role=val('speakerRole')||'speaker';const relationship=val('relationship');const tone=val('tone')||'warm';const length=val('length')||'2 minutes';const intro=tone==='formal'?`Good evening. As ${role}, I am honoured to celebrate ${names}.`:`Hello everyone. I am here as ${role}, and it means a great deal to celebrate ${names} with all of you.`;const relation=relationship?`My relationship with the couple has taught me the value of ${sentence(relationship).toLowerCase()}`:'';const memory=val('memory')?`One memory I would like to share is this: ${sentence(val('memory'))}`:'';const qualities=val('qualities')?`What I admire most is ${sentence(val('qualities')).toLowerCase()}`:'';const thanks=val('thanks')?`I would also like to thank ${sentence(val('thanks'))}`:'';const closing=val('closing')==='blessing'?`May ${names} meet the years ahead with patience, laughter and a steady commitment to one another.`:`Please join me in celebrating ${names} and the life they are building together.`;output.textContent=[`Draft for approximately ${length}`,intro,relation,memory,qualities,thanks,closing].filter(Boolean).join('\n\n')}
form.addEventListener('submit',build);form.addEventListener('input',build);form.addEventListener('reset',()=>setTimeout(build,0));speechBuilder.querySelector('[data-builder-print]')?.addEventListener('click',()=>{build();window.print()});build()}
